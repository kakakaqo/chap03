/**
 * 실행 클래스
 * - main 메소드가 있어서 실행할 수 있는 클래스
 * - 라이브러리 클래스를 객체로 만들어서 이용하는 클래스
 */

package class01; // 패키지 - 폴더

public class CarMain {
	
	public static void main(String[] args) {
		// Car 클래스 객체 생성
		Car car = new Car();
	// 클래스이름(앞 대문자) 변수(앞 소문자) new 클래스();
	}
}